import { StyleSheet } from 'react-native';


const uStyles = StyleSheet.create({
    dropShadow: {
        shadowColor: "#000",
        shadowOffset: {
          width: 0,
          height: 1,
        },
        shadowOpacity: 0.25,
        shadowRadius: 4.00,
    
        elevation: 1,
      },
});

export default uStyles;